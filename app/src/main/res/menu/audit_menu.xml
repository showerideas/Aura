<?xml version="1.0" encoding="utf-8"?>
<!--
  Toolbar menu for the AuditFragment (Phase 6.6).
  Export and Clear actions live here so the RecyclerView has full viewport height.
-->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_export_audit"
        android:title="@string/audit_export_csv"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_clear_audit"
        android:title="@string/audit_clear"
        app:showAsAction="never" />

</menu>
