<?xml version="1.0" encoding="utf-8"?>
<!--
  A single profile row in the profile switcher bottom sheet.
  Active profile shows a filled chip; others show an outlined "Set as active" button.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingVertical="12dp">

    <!-- Profile type badge chip -->
    <com.google.android.material.chip.Chip
        android:id="@+id/chip_profile_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="12dp"
        android:checkable="false"
        android:clickable="false"
        android:focusable="false"
        android:textSize="12sp" />

    <!-- Name + hint column -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_profile_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/on_surface"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_profile_hint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="@string/profile_card_hint"
            android:textColor="@color/on_surface_secondary"
            android:textSize="12sp"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Active badge (shown when this profile is active) -->
    <TextView
        android:id="@+id/tv_active_badge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:background="@drawable/chip_background"
        android:paddingHorizontal="8dp"
        android:paddingVertical="4dp"
        android:text="@string/profile_active"
        android:textColor="@color/aura_cyan"
        android:textSize="12sp"
        android:visibility="gone" />

    <!-- Delete button (shown on long press via ItemTouchHelper or context menu) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_delete_profile"
        style="@style/Widget.MaterialComponents.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/profile_delete"
        android:text="@string/profile_delete"
        android:textColor="@color/error"
        android:textSize="12sp"
        android:visibility="gone" />

</LinearLayout>
