<?xml version="1.0" encoding="utf-8"?>
<!--
  Exchange audit log screen (Phase 6.6).
  Timeline of all exchange events sourced from ExchangeAuditEntry rows.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Toolbar -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar_audit"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/surface"
            app:title="@string/audit_title"
            app:titleTextColor="@color/on_surface"
            app:navigationIcon="@drawable/ic_back"
            app:menu="@menu/audit_menu" />

        <!-- Empty state -->
        <TextView
            android:id="@+id/tv_audit_empty"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/audit_empty"
            android:textColor="@color/on_surface_secondary"
            android:textSize="16sp"
            android:visibility="gone" />

        <!-- Entry list -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_audit"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:paddingHorizontal="16dp"
            android:clipToPadding="false" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
