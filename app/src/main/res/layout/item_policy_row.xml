<?xml version="1.0" encoding="utf-8"?>
<!--
  Single MDM policy row: label on the left, enforced value on the right.
  Included multiple times by fragment_enterprise_settings.xml.
  The fragment populates the sibling hidden TextViews (tv_max_attempts, etc.)
  directly via ViewBinding; these rows provide the visual chrome.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="48dp"
    android:orientation="horizontal"
    android:paddingHorizontal="8dp"
    android:paddingVertical="12dp"
    android:gravity="center_vertical">

    <TextView
        android:id="@+id/tvPolicyLabel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textAppearance="?attr/textAppearanceBodyMedium" />

    <TextView
        android:id="@+id/tvPolicyValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:textAppearance="?attr/textAppearanceBodySmall"
        android:textColor="?attr/colorPrimary" />

</LinearLayout>
